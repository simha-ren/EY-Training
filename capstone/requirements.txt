# Frontend
streamlit>=1.36
streamlit-option-menu>=0.3.2

# Backend & API
fastapi>=0.104
uvicorn>=0.24
pydantic>=2.0
python-multipart>=0.0.6

# Claude LLM
anthropic>=0.25.0
openai>=1.30      # used for Groq (OpenAI-compatible) backend

# --- Vector DB (retrieval-augmented answers) ---
faiss-cpu>=1.7          # local vector store (default when present)
pinecone>=4.0          # uncomment to use Pinecone cloud vector DB
# sentence-transformers>=2.2  # optional: semantic embeddings for faiss/pinecone

# File Processing
pypdf>=4.0
python-docx>=0.8.11
openpyxl>=3.0
python-pptx>=0.1.3

# Database & ORM
sqlalchemy>=2.0
psycopg2-binary>=2.9
alembic>=1.12

# Data Processing
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
pyyaml>=6.0

# Observability & Logging
python-json-logger>=2.0
prometheus-client>=0.19
opentelemetry-instrumentation>=0.43b0
opentelemetry-api>=1.21
opentelemetry-sdk>=1.21
opentelemetry-exporter-jaeger>=1.21

# Report Generation
reportlab>=4.0
python-dotenv>=1.0
jinja2>=3.0

# Utilities
langchain>=0.1.0
langchain-community>=0.0.10
langchain-anthropic>=0.1.0
requests>=2.31
aiohttp>=3.9
python-dateutil>=2.8

# --- Testing & coverage (powers the Tests & Coverage tab) ---
pytest>=8.0
coverage>=7.4

# --- Open-source RAG backends (free, self-hosted) ---
qdrant-client>=1.9
# pgvector backend (optional; needs a Postgres server):
# psycopg2-binary>=2.9
# pgvector>=0.2

# --- Auth (stdlib pbkdf2 — no extra dep) + optional LLM tracing ---
# LangSmith tracing (optional): set LANGCHAIN_TRACING_V2=true + LANGCHAIN_API_KEY
langsmith>=0.1
# Langfuse tracing (optional, open-source): set LANGFUSE_PUBLIC_KEY + LANGFUSE_SECRET_KEY
# langfuse>=2.0

# --- Load testing (dev/CI; open-source) ---
# locust>=2.20        # pip install locust ; or use k6 (tests/load/k6.js)

# --- Azure ops (optional; installed for Azure deployment) ---
azure-monitor-opentelemetry>=1.6     # Application Insights observability
azure-communication-email>=1.0       # email notifications
azure-servicebus>=7.11               # async job queue (worker.py)
azure-identity>=1.16                 # managed-identity auth for Service Bus/ACR
langgraph>=0.2                       # agent orchestration (StateGraph pipeline)
azure-ai-documentintelligence>=1.0  # PDF/doc layout, tables, figures (optional)
pymupdf>=1.24                        # render PDF pages to images for visual analysis
redis>=5.0                          # optional: shared rate-limit store when REDIS_URL is set
pyodbc>=5.1                          # Azure SQL tracking DB in production (needs MS ODBC Driver 18)

# --- RAGAS evaluation ---
# LLM-graded RAGAS metrics run out-of-the-box via your Claude/Groq client (no extra deps).
# The FULL `ragas` library is OPTIONAL and pins an older langchain-core that conflicts
# with the langgraph version used here, so it is NOT installed by default. To use it,
# create a SEPARATE evaluation environment:
#   pip install ragas langchain-anthropic langchain-huggingface
# and set CLAUDE_API_KEY (or OPENAI_API_KEY); ragas_llm.py auto-detects and uses it,
# otherwise it falls back to LLM-graded-via-client, then to a heuristic.
matplotlib>=3.7
httpx>=0.24,<0.28        # required by FastAPI/Starlette TestClient (tests/test_rate_limiter.py)
